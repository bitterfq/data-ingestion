numpy
pandas
faker
ulid-py
pyarrow
pytest
minio
boto3
python-dotenv

# Bridge service dependencies  
kafka-python
requests
urllib3

# Spark & Iceberg pipeline dependencies
pyspark==3.5.1
# If using Iceberg with Spark, you may need to provide the Iceberg JAR separately,
# but for pip installs, pyspark is sufficient for most local dev/test.
psycopg2-binary